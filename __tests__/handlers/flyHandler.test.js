
describe('Handlers', () => {
  describe('flyHandler', () => {
    it('should return fly data with full query description', () => {

    });

    it('should return fly data without origin direct', () => {

    });

    it('should return fly data without destination direct', () => {

    });

    it('should return fly data without both direct', () => {

    });

    it('should return fly data without date', () => {

    });
  });
});
